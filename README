# RootTools

This is set of useful (for me) tools to use together with ROOT (http://root.cern.ch)

# Compialtion/Installation
git clone https://github.com/dziadu/RootTools
mkdir RootTools/build
cd RootTools/build
cmake ..
make
make install # optional
